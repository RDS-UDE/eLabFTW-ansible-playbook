################################################################################
#  WARNING: This file needs to be vaulted (encrypted) for use in production    # 
################################################################################

# TODO: When using a DFN (or similar) certificate overwrite the content of
# this file with the secret key file e.g. one generated through the script run
# by the base_config role.
# To keep this key secret on the ansible control node, encrypt this file using
# ansible-vault. Never commit your secrets in plain text to a repository!
